const path = require("path");

module.exports = {
  config: path.resolve("src", "config", "index.js"),
  "models-path": path.resolve("src", "modles", "**", "model", "index.js"),
  "seeders-path": path.resolve("src", "db", "seeders"),
  "migrations-path": path.resolve("src", "db", "migrations"),
};
